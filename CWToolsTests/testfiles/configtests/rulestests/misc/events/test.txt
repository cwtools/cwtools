namespace = test

country_event = {
    option = {
        #error
        ship_only = test
        OR = {
            OR = {
                #error
                ship_only = test
                #error
                as = as
            }
        }
        test = {
            test = {
                #error
                ship_only = test
                #error
                as = as
            }
        }
    }
}
