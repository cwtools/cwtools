test_scripted_effect_ship = {
    OR = {
        set_ship_flag = yes
    }
}

test_scripted_effect_country = {
    OR = {
        set_country_flag = yes
    }
}

test_scripted_effect_none = {
    test = {
        test_scripted_effect_params = {
               test_lhs = OR
               test_rhs = yes
           }

           test_scripted_effect_params = {
               test_lhs = blah
               test_rhs = blah
           }
    }
}

test_scripted_effect_params = {
    #error
    #error
    $test_lhs$ = {

    }
    #error
    #error
    set_ship_flag = $test_rhs$
}

test_scripted_effect_optional_params = {
    set_country_flag = $optional_param|test_flag$
}

test_scripted_effect_not_optional_params = {
    #error
    set_country_flag = $not_optional_param$
}

test_scripted_effect_several_params = {
    #error
    set_country_flag = $not_optional_param$
    set_country_flag = $optional_param|test_flag$
}