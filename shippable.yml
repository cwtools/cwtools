language: none
build:
  pre_ci_boot:
    image_name: dcurylo/fsharp-mono-netcore
    image_tag: latest
    pull: true
  ci: 
    - cd ./CK2EventsUI
    - dotnet build
    - dotnet electronize build win
