language: none
build:
  pre_ci_boot:
    image_name: dcurylo/fsharp-mono-netcore
    image_tag: latest
    pull: true
    options: "-e HOME=/root -v `pwd`:/root/src --rm -it bash"
  ci: 
    dotnet build
    exit
