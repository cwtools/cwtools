types = {
    type[omen] = {
        path = "game/common/omens"

        localisation = {
            name = "$"
            description = "$_desc"
            # _TAG, _religion
        }
    }
}

## replace_scope = { this = country root = country }
omen = {
    ## cardinality = 1..inf
    alias_name[modifier] = alias_match_left[modifier]
}