image: mcr.microsoft.com/dotnet/core/sdk:3.0

variables:
  DOCKER_DRIVER: overlay2
  GIT_DEPTH: "3"

stages:
  - test

test:
    stage: test
    script:
    - ls -A
    - chmod +x ./build.sh
    - /bin/sh ./build.sh Test
